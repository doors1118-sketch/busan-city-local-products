# 작업 로그 - 2026-03-16 (사무실에서 작업)

## 오늘 완료한 작업

### 1. 집 작업분 동기화
- GitHub에서 `git pull` → 집에서 작업한 2개 커밋 반영
  - `ed16f67`: `daily_pipeline_sync.py`에 `import os` 추가
  - `1e529ab`: 보호제도 테이블 텍스트 확대, 미적용 계약 상세 expander 추가, 경제효과 카드 개선

### 2. 대시보드 UI 수정
- `dashboard.py`: "지역총생산 부가가치 증가" 라벨이 2줄로 줄바꿈되는 문제 수정
  - `white-space: nowrap` 추가 + 텍스트 축약 → 1줄 표시

### 3. 보호제도 공사 필터 버그 수정 (중요)
- **문제**: 정부 미적용 계약 상세에 부산외 지역 공사가 대부분 포함됨
- **원인**: 수주율 계산은 `filter_cnstwk_by_site` + `is_non_busan_contract` 2단계 필터인데,
  보호제도 분석은 `site_map`만 사용하여 전화번호/계약명 키워드 필터 누락
- **수정**: `build_api_cache.py` 보호제도 공사 분석에 `is_non_busan_contract` 필터 추가
  - 전화번호 체크 (051/070/010 외 → 타지역 판별)
  - 계약명 키워드 체크 (타지역 지명 포함 시 배제)
  - 낙찰정보/지역제한 bypass 로직 동일 적용

### 4. 서버 배포
- 배포 방식: 5분 자동 pull → **수동 pull로 전환** (집에서 변경)
- NCP 웹 콘솔에서 수동 배포 완료:
  - `git pull origin main` → `python3 build_api_cache.py` → `systemctl restart busan-api`
- 캐시 재생성 완료 (수정된 필터 적용)

### 5. PROJECT_STATUS.md 업데이트
- 서버 배포 방식 자동→수동 전환 기록

## 커밋 이력
| 커밋 | 내용 |
|------|------|
| `96e3fac` | dashboard: 부가가치 라벨 1줄 표시 + PROJECT_STATUS 배포방식 수동전환 기록 |
| `be92e0b` | fix: 보호제도 공사분석에 is_non_busan_contract 필터 추가 |

## 남은 작업
- 대시보드 보호제도 탭 숫자 검증 (수의계약 분리 표시 검토)
- 기관별 검색 + 엑셀 다운로드 기능
- 서버 `alert_config.json` 배포 + 알림 채널 활성화
